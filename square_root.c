#include<stdio.h>
#include<math.h>
int main(){
    int num;
    printf("Enter a number to get the square root\n",num);
    scanf("%d", &num);
    printf("Square root of number is = %f", sqrt(num));
    return 0;
}